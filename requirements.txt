qiskit==1.0.2
qiskit-aer==0.13.3
numpy==1.26.4
scipy==1.17.1
matplotlib==3.8.4
# pylatexenc (Fase 2, tarea 2.9): dependencia OPCIONAL del dibujante
# matplotlib de Qiskit. Sin ella, qc.draw("mpl") -el circuito de Shor de la
# figura 3 y del README- revienta con MissingOptionalLibraryError. Es Python
# puro, no compila nada, y Qiskit no la arrastra por defecto.
pylatexenc==2.10
liboqs-python==0.16.0
cryptography==48.0.1
streamlit==1.33.0
# pyarrow es dependencia transitiva de streamlit; sin pinear, pip resolvia
# la 25.x (año y medio posterior a streamlit 1.33), que segfaultea al
# serializar DataFrames en st.dataframe (reproducido el 2026-07-18 con la
# tabla de sifting del dashboard). 16.1.0 es la contemporanea de 1.33.
pyarrow==16.1.0
# numba: retirada en la tarea 4.11 (cierra la deuda I2, abierta desde la
# Fase 3). Cero usos en todo el repositorio, confirmado de nuevo antes de
# quitarla (`grep -rn numba src/ tests/ scripts/ dashboard/`). numpy se
# mantiene fijado explicitamente en numpy==1.26.4 arriba, asi que la
# resolucion de dependencias no cambia; lo unico que se pierde es la
# atadura adicional que numba imponia sobre el rango de numpy.
#
# seaborn ya se habia quitado en un pase anterior: mismo motivo, cero usos.
# MEJORA D4 (cierra I7): aqui habia un pytest==7.4.4 mientras
# requirements-dev.txt fija pytest==8.4.0, asi que la version dependia de
# DONDE se corrieran los tests. pytest es herramienta de desarrollo, igual
# que ruff/black/mypy: vive solo en requirements-dev.txt, que el Dockerfile
# y la CI ya instalan. Una sola declaracion, nada que sincronizar a mano.
